name: fhir_auth
description: A Dart/Flutter package for authorization & authentication, includes SMART on FHIR and Google sign-in
version: 0.11.0
homepage: https://www.fhirfli.dev/
repository: https://github.com/MayJuun/fhir/tree/main/fhir_auth

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  crypto: ^3.0.3
  collection: ^1.17.2
  fhir: ^0.11.4
  # fhir: 
  #   path: ../fhir
  flutter:
    sdk: flutter
  # flutter_web_plugins:
  #   sdk: flutter
  flutter_web_auth_2: ^2.2.1
  # flutter_web_auth_2:
  #   git:
  #     url: https://github.com/FireJuun/flutter_web_auth_2.git
  #     ref: fix_macos_example
  #     path: flutter_web_auth_2
  freezed_annotation: ^2.4.1
  google_sign_in: ^6.1.5
  http: ^1.1.0
  http_parser: ^4.0.2
  jwt_decoder: ^2.0.1
  oauth2: ^2.0.2
  oauth2_client: ^3.2.2
  url_launcher: ^6.1.14

dev_dependencies:
  build_runner: ^2.4.6
  flutter_lints: ^2.0.3
  freezed: ^2.4.3
  import_sorter: ^4.6.0
  test: ^1.24.7

